/*!
 * Mocks
 */

/**
 * Module Dependencies
 */

var EventEmitter = require('./event-emitter-mock'),
    Mocks = {
      EventEmitter: EventEmitter
    };

/**
 * Module Exports
 */

exports = module.exports = Mocks;